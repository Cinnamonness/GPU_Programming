# Задание 4. Лабораторная работа №4

---
## Оглавление
- [Свойства видеокарты:](#свойства-видеокарты)
   -[Пояснения к полученным параметрам](#пояснения-к-полученным-параметрам)
- [Формулировка задания по Лабораторной работе 4](#формулировка-задания-по-Лабораторной-работе-4)\
- [Параллельное создание матрицы на GPU](#Параллельное-создание-матрицы-на-GPU)
   -[Ключевые особенности реализации](#Ключевые-особенности-реализации)
   -[Конфигурация вычислений на GPU](#Конфигурация-вычислений-на-GPU)
   -[Результаты выполнения](#Результаты-выполнения)
   -[Анализ производительности](#Анализ-производительности)
   -[Ускорения вычислений](#Ускорения-вычислений)
   -[Расходы на передачу данных](#Расходы-на-передачу-данных)
- [Транспонирование матрицы на GPU](#Транспонирование-матрицы-на-GPU)

---

# Свойства видеокарты

Получим от видеокарты информацию о её возможностях.  
Для этого используем функцию **cudaGetDeviceProperties()**, принимающую в качестве параметра номер видеокарты и возвращающую в структуре **cudaDeviceProp** интересующие нас значения.  

Зпускаем скомпилированный **.\outputs\device_info.exe**

```bash
Device has Compute Capability 8.6
Device name : NVIDIA GeForce RTX 3080
Total global memory : 10239 MB
Shared memory per block : 49152
Registers per block : 65536
Warp size : 32
Memory pitch : 2147483647
Max threads per block : 1024
Max threads dimensions : x = 1024, y = 1024, z = 64
Max grid size: x = 2147483647, y = 65535, z = 65535
Clock rate: 1710.00 MHz
Total constant memory: 65536
Compute capability: 8.6
Texture alignment: 512
Device overlap: 1
Multiprocessor count: 68
Kernel execution timeout enabled: true
```

---

### Пояснения к полученным параметрам

- Device has Compute Capability — вычислительная способность GPU, определяет поколение архитектуры (важно при выборе версии CUDA и совместимости).

- Device name - название видеокарты.

- Total global memory - объём глобальной памяти GPU.

- Shared memory per block - объём разделяемой памяти, доступной на один блок потоков.

- Registers per block - количество регистров, доступных на блок.

- Warp size - размер варпа (группы потоков, исполняемых одновременно).

- Max threads per block - максимальное число потоков в одном блоке.

- Max threads dimensions - допустимые размеры блока по осям X, Y, Z.

- Max grid size - максимальные размеры сетки блоков.

- Clock rate - тактовая частота ядра.

- Total constant memory - объём постоянной памяти.

- Compute capability - вычислительная способность GPU.

- Texture alignment - выравнивание текстур в памяти.

- Device overlap - поддержка перекрытия вычислений и копирования данных.

- Multiprocessor count - количество мультипроцессоров (SM) на устройстве.

- Kernel execution timeout enabled - наличие ограничения времени выполнения ядер.

---

## Формулировка задания по Лабораторной работе 4

Разобрать программы из глав 6, 7 методического пособия, запустить, замерить время, сравнить по производительности, уметь объяснять работу программ детально.

---

## Параллельное создание матрицы на GPU

Программа демонстрирует сравнение производительности CPU и GPU при создании большой квадратной матрицы размером 5000 * 5000 элементов.

Каждый элемент вычисляется по формуле: A[i][j]=10 * i * j

* CUDA ядро `createMatrix` - параллельное создание матрицы на GPU

* CPU функция `createMatrixCPU` - последовательное создание матрицы на CPU

* Система измерения времени с использованием событий CUDA

* Проверка корректности результатов вычислений

---

### Ключевые особенности реализации

| Этап                                | Описание                                                                                               |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Инициализация                   | Определяются параметры задачи: размер матрицы, объем данных, создаются CUDA-события для замера времени |
| Создание матрицы на CPU         | Последовательное заполнение матрицы в двойном цикле                                                    |
| Выделение памяти на GPU         | Используется `cudaMalloc` для выделения памяти под матрицу                                             |
| Создание матрицы на GPU         | Запуск CUDA-ядра `createMatrix` в 1000 итераций                                                        |
| Копирование данных с GPU на CPU | Используется `cudaMemcpy`                                                                              |
| Проверка корректности           | Сравнение 100 случайных элементов матриц CPU и GPU                                                     |
| Анализ времени                  | Сравнение времени выполнения на CPU и GPU, расчет ускорения                                            |
| Очистка ресурсов                | Освобождение памяти и уничтожение CUDA-событий                                                         |


**Параллельная архитектура GPU**

``` C
dim3 threadsPerBlock(16, 16);        // 256 потоков в блоке
dim3 blocksPerGrid(313, 313);        // 97 969 блоков
// Всего: 25 080 064 потоков для матрицы 25 000 000 элементов
```

**Многократное выполнение**

``` C
const int iterations = 1000;
for (int iter = 0; iter < iterations; iter++) {
    createMatrix<<<blocksPerGrid, threadsPerBlock>>>(d_B, n);
}
```

---

### Конфигурация вычислений на GPU

| Параметр                 | Значение        | Описание                                    |
| ------------------------ | --------------- | ------------------------------------------- |
| Размер матрицы           | 5000 * 5000     | Всего 25 млн элементов                      |
| Размер данных            | 95.37 MB        | Объем в байтах для одной матрицы            |
| Размер блока             | 16 * 16 потоков | 256 потоков в блоке                         |
| Количество блоков        | 313 * 313       | Покрывают всю матрицу                       |
| Общее число потоков      | 25 080 064      | ~1 поток на элемент                         |
| Количество итераций ядра | 1000            | Повторение для статистической достоверности |

---

### Результаты выполнения

| Этап                            | Время (мс)     | Доля от общего GPU-времени |
| ------------------------------- | -------------- | -------------------------- |
| Выделение памяти на GPU         | 6.105088       | 3.2%                       |
| Выполнение ядра (1000 итераций) | 139.898849     | 72.9%                      |
| Копирование данных GPU -> CPU    | 45.549023      | 23.7%                      |
| Прочие операции                 | 0.268635       | 0.1%                       |
| **Итого (GPU)**                 | **191.821594** | 100%                       |
| **CPU (одна матрица)**          | **50.648064**  | —                          |

---

### Анализ производительности

| Показатель                    | Значение   |
| ----------------------------- | ---------- |
| CPU (1 матрица)               | 50.648 мс  |
| GPU (всего, с накладными)     | 191.822 мс |
| GPU (вычисления, 1000 матриц) | 139.899 мс |
| GPU (в среднем на матрицу)    | 0.1399 мс  |

---
### Ускорения вычислений

| Тип ускорения                               | Расчет                    | Значение    |
| ------------------------------------------- | ------------------------- | ----------- |
| Общее ускорение (CPU / GPU общее)           | 50.648 / 191.822          | **0.264×**  |
| Ускорение вычислений (CPU / GPU на матрицу) | 50.648 / 0.1399           | **362.0×**  |
| Ускорение за 1000 итераций                  | (50.648 × 1000) / 139.899 | **≈362.0×** |

---
### Расходы на передачу данных

| Направление                    | Объем     | Время (мс) |
| ------------------------------ | --------- | ---------- |
| CPU -> GPU                      | 95.37 MB  | 6.105      |
| GPU -> CPU                      | 95.37 MB  | 45.549     |
| **Итого**                      | 190.74 MB | **51.654** |
| **Доля от общего GPU-времени** | —         | **26.9%**  |


* GPU быстрее CPU в 362 раза по времени чистых вычислений.

* Однако при учёте накладных расходов (передача данных и выделение памяти) общее ускорение снижается.

* Почти четверть времени GPU уходит на обмен данными с хостом.

* Программа корректно проверяет результаты вычислений и демонстрирует полное совпадение данных CPU и GPU.

---

## Транспонирование матрицы на GPU

Программа реализует параллельное транспонирование матрицы на GPU с использованием CUDA и сравнивает производительность с CPU-версией.

В ходе выполнения измеряется:

* время транспонирования на CPU и GPU;

* затраты на копирование данных между CPU <-> GPU;

* ускорение вычислений на GPU относительно CPU.

---

### Основные параметры

| Параметр                    | Значение    |
| --------------------------- | ----------- |
| Размер матрицы (исходный)   | 1000 * 2000 |
| Размер блока CUDA           | 16 * 16     |
| Выровненные размеры матрицы | 1008 * 2000 |
| Количество элементов        | 2 016 000   |
| Общий размер данных         | 7.69 MB     |
| Количество итераций ядра    | 500         |

---
### Ключевые компоненты кода

**Ядро CUDA** - параллельно копирует элементы матрицы A в транспонированное расположение AT. Каждый поток обрабатывает один элемент.

``` C
__global__ void matrixTranspose(const BASE_TYPE *A, BASE_TYPE *AT, int rows, int cols)
{
    int iA = cols * (blockDim.y * blockIdx.y + threadIdx.y)
           + blockDim.x * blockIdx.x + threadIdx.x;
    int iAT = rows * (blockDim.x * blockIdx.x + threadIdx.x)
            + blockDim.y * blockIdx.y + threadIdx.y;
    AT[iAT] = A[iA];
}
```

**Функция CPU-версии** - последовательное транспонирование для сравнения производительности и проверки корректности результата GPU.

``` C
void matrixTransposeCPU(const BASE_TYPE *A, BASE_TYPE *AT, int rows, int cols)
{
    for (int i = 0; i < rows; i++)
        for (int j = 0; j < cols; j++)
            AT[j * rows + i] = A[i * cols + j];
}
```

**Выравнивание размеров** - выравнивание размеров

Обеспечивает кратность размеров блока BLOCK_SIZE (для оптимального распределения потоков).

``` C
int toMultiple(int a, int b)
```

---

### Результаты измерений

| Этап                                   | Время (мс) | Доля (%) |
| -------------------------------------- | ---------- | -------- |
| Работа с памятью (копирование CPU <-> GPU) | 9.359      | 16.5%    |
| Выполнение ядра (500 итераций)         | 0.134      | 0.2%     |
| Транспонирование на CPU                | 8.221      | 14.5%    |
| Прочие операции                        | 38.968     | 68.7%    |
| **Итого (общее время)**                | **56.682** | **100%** |

В результате проверки корректности транспонирования: транспонирование выполнено правильно.

**Сравнение производительности**

| Показатель          | CPU    | GPU (ядро) | GPU (всё с накладными) |
| ------------------- | ------ | ---------- | ---------------------- |
| Время (мс)          | 8.2207 | 0.1342     | 56.682                 |
| Ускорение (CPU/GPU) | —      | **×61.27** | **×0.17**              |

---
### Выводы

1. Корректность подтверждена - результаты CPU и GPU совпадают.

2. CUDA-ядро работает значительно быстрее при чистом вычислении: ~61× быстрее CPU.

3. Общее ускорение не достигнуто, т.к. накладные расходы на передачу данных и подготовку ядра существенно превышают время вычислений.
